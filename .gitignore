node_modules/
dist/
dist-ssr/
*.local
coverage/

# Vite / PWA build output
dev-dist/
public/wasm/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# ---------------------------------------------------------------------------
# Never commit key material or real personal data.
#
# test/vectors/ IS committed: those are fixed, published, throwaway values and
# are the cross-platform interop regression guard (docs/qr-payload-format.md
# section 8). Anything below is real material and must stay out of history.
# ---------------------------------------------------------------------------
*.pem
*.p12
*.key
secrets.json
.env
.env.*
scratch/
**/real_card_*.json

# Claude Code: share project config, keep personal overrides local
.claude/settings.local.json
